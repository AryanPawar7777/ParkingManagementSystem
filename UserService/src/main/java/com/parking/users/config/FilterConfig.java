package com.parking.users.config;

// import org.springframework.boot.web.servlet.FilterRegistrationBean;
// import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class FilterConfig {

    // @Bean
    // public FilterRegistrationBean<ServiceAuthFilter> registerAuthFilter(ServiceAuthFilter authFilter) {
    //     FilterRegistrationBean<ServiceAuthFilter> registration = new FilterRegistrationBean<>();
    //     registration.setFilter(authFilter);
    //     registration.addUrlPatterns("/*");
    //     registration.setOrder(1);
    //     return registration;
    // }
}
